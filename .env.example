# Spoonacular API Configuration
SPOONACULAR_API_KEY=your_spoonacular_api_key_here

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/flavor_table

# JWT Authentication
JWT_SECRET=your_super_secure_jwt_secret_key_here

# CORS Configuration (for production)
FRONTEND_URL=http://localhost:3000

# Optional: Database SSL Configuration (for production)
# DB_SSL=true

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual configuration
# 3. Get your Spoonacular API key from: https://spoonacular.com/food-api
# 4. Generate a secure JWT secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 5. Update DATABASE_URL with your PostgreSQL connection details
# 6. For production deployment, set NODE_ENV=production and update FRONTEND_URL
